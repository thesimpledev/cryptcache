package main

func projectHandler(verb string, args []string) {
	//Create
}

func profileHandler(verb string, args []string) {
	//create, delete, export, diff, privaet key path, public key path, private key, public key
}

func secretHandler(verb string, arges []string) {
	//create, delete, update, view
	//encrypted vs not encrypted
}
